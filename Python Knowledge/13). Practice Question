#practice quesions

#ques1). w.a.f to print length of list (list is the parameter)
"""
cities = ["delhi", "noida", "chennai", "gurugram"]
heroes = ["thor", "ironman", "captain america"]

print(heroes[0])
print(heroes[1])

def print_len(lst):
    print(len(lst))

print_len(cities)
print_len(heroes)


#w.a.f to print the elements of a list in a single line
def print_list(lst):
    for item in lst:
        print(item, end=" ")
    print()  # Add this line to print a newline character

print_list(cities)
print_list(heroes)
"""

#w.a.f to find the factorial of n (n is parameter)
"""
def cal_fact(n):
    fact=1
    for i in range(1, n+1):
        fact*=i
    print(fact)
cal_fact(5)
"""

#w.a.f to convert usd to inr
"""
def converter(usd_val):
    inr_val = usd_val * 83
    print(usd_val, "USD =", inr_val, "INR")

converter(74)
"""
