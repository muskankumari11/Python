#tuple
#w.a.p to count the number of students with the "A" grade in the following tuple.
"""grade=["C", "D", "B", "A", "A", "B", "A"]
print(grade.count("A"))"""

#store the above value in a list and sort them from "A" to "D"
grade=["C", "D", "B", "A", "A", "B", "A"]
grade.sort()
print(grade)
